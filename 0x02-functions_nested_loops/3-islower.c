#include "main.h"

/**
 * _islower - checks for lowercase character
 * @c:The character in letters
 * Return: 1 and  0 depending the condition
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
